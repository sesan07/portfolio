import { AnimateEntryDirective } from './animate-entry.directive';

describe('AnimateEntryDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateEntryDirective();
    expect(directive).toBeTruthy();
  });
});
