import { ProjectImageDirective } from './project-image.directive';

describe('ProjectImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ProjectImageDirective();
    expect(directive).toBeTruthy();
  });
});
